# test
name: first test
on: [push]

jobs:
  run-shell:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "Hello world!"
      - name: multiple commands
        run: |
          node -v
          npm -v  